import React from "react";

// Native-base components
import { Box, ScrollView, Text } from "native-base";

const ProductDetail = () => {
  return (
    <ScrollView>
      <Box>
        <Text>Producy Detail</Text>
      </Box>
    </ScrollView>
  );
};

export default ProductDetail;
