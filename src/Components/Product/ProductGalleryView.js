import React from "react";

// Native-base components
import { Box, ScrollView, Text } from "native-base";

const ProductGalleryView = () => {
  return (
    <ScrollView>
      <Box>
        <Text>Product Gallery View</Text>
      </Box>
    </ScrollView>
  );
};

export default ProductGalleryView;
