import React from "react";

// Native-base components
import { Box, ScrollView, Text } from "native-base";

const ProductRegistrationForm = () => {
  return (
    <ScrollView>
      <Box>
        <Text>Product Registration Form</Text>
      </Box>
    </ScrollView>
  );
};

export default ProductRegistrationForm;
